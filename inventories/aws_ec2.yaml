plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  tag:Deploy: "yes"

keyed_groups:
  - key: tags.Name
    prefix: tag_Name

compose:
  deploy: tags.Deploy
  name: tags.Name
  env: tags.Environment
